import 'package:flutter/material.dart';

class ChatMessageBody extends StatelessWidget {
  const ChatMessageBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
